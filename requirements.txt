# Core dependencies
# SQLite is built into Python, no external dependency needed

# GUI framework
PyQt6>=6.5.0

# Process detection (for checking if browsers are running)
psutil>=5.9.0

# Future dependencies (not yet used)
# SQLAlchemy>=2.0.0      # ORM (optional, using raw SQLite for now)
# pydantic>=2.0.0        # Data validation
# platformdirs>=3.0.0    # Cross-platform paths
# beautifulsoup4>=4.12.0 # HTML parsing
# requests>=2.31.0       # HTTP requests for metadata
